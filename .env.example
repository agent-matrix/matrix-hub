# ==============================================================================
# Matrix Hub — environment template for local dev (docker compose + uvicorn)
# Copy to ".env" and adjust as needed.
# ==============================================================================

# -------------------------
# Server
# -------------------------
APP_NAME=matrix-hub
HOST=0.0.0.0
PORT=7300
LOG_LEVEL=INFO

# Optional admin API token for protected endpoints (leave blank to disable auth)
API_TOKEN=

# -------------------------
# Database
# Use Postgres in compose by default; for local single-file mode you can use:
#   DATABASE_URL=sqlite+aiosqlite:///./data/catalog.sqlite
# -------------------------
DATABASE_URL=sqlite:///./data/catalog.sqlite

# -------------------------
# Search & ranking (simple defaults; can be extended later)
# -------------------------
SEARCH_LEXICAL_BACKEND=pgtrgm          # options: pgtrgm | none
SEARCH_VECTOR_BACKEND=none             # options: pgvector | milvus | none
SEARCH_HYBRID_WEIGHTS=sem:0.6,lex:0.4,rec:0.1,q:0.1
RAG_ENABLED=false
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2

# -------------------------
# Catalog ingestion
# Comma-separated list of remote index.json URLs
# -------------------------
CATALOG_REMOTES=https://raw.githubusercontent.com/agent-matrix/catalog/main/index.json


# Polling interval in minutes (ingestor scheduler)
INGEST_INTERVAL_MIN=15

# -------------------------
# MCP Gateway integration
# -------------------------
MCP_GATEWAY_URL=http://mcpgateway:7200
MCP_GATEWAY_TOKEN=supersecret

# -------------------------
# Misc
# -------------------------
PIP_INDEX_URL=
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1
